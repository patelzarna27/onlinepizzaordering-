export class Pizza {
    id: string;
    pizza_Type: string; // thin crust, hand toasted
    pizza_Size: string; // small, medium, extra large
    pizza_Price: string;
    isEditable: boolean;
  //
}

